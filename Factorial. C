#INCLUDE<STDIO.H>

INT FACTORIAL (INT N) {

IF (N == 0){

RETURN 1:

} RETURN N* FACTORIAL(N-1):
{

INT MAIN() {

INT NUM:

PRINTF("ENTER A NUMBER: "): SCANF("%D", &NUM):

PRINTF("FACTORIAL = % DIN".

FACTORIAL(NUM)):

RETURN 0:
}
