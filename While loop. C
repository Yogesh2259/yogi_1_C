#include <stdio.h>
int main() {
    int n, i = 1;sum=0
    printf("Enter the value of n: ");
    scanf("%d", &n) ;
    while (i <= n) {
        printf("%d ", i);
        sum+=I;
        i++;  
    }
    return 0;
}
